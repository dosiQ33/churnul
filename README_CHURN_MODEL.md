# 📊 МОДЕЛЬ ПРОГНОЗИРОВАНИЯ ОТТОКА КЛИЕНТОВ (CHURN MODEL)

## Полный воспроизводимый пайплайн для банка

---

## 📋 Описание

Данный проект содержит полный пайплайн построения моделей прогнозирования оттока клиентов банка, соответствующий требованиям регулятора.

### ✨ Ключевые особенности:

1. **Две модели по сегментам:**
   - **Модель 1:** Малый бизнес (SMALL_BUSINESS)
   - **Модель 2:** Средний + Крупный бизнес (MIDDLE + LARGE_BUSINESS)

2. **Временное разбиение (Temporal Split):**
   - Train: 70%
   - Validation: 15%
   - Test (OOT - Out-of-Time): 15%

3. **Полная воспроизводимость:**
   - Fixed random_seed = 42
   - Детерминированное разбиение по времени
   - Run All → идентичные результаты

4. **Соответствие требованиям банка:**
   - Все метрики: GINI, ROC-AUC, PR-AUC, Accuracy, Precision, Recall, F1-Score
   - Полная документация preprocessing
   - Сохранение всех параметров

---

## 📁 Структура файлов

```
churnul/
├── Churn_Model_Complete.ipynb          # ГЛАВНЫЙ NOTEBOOK (воспроизводимый pipeline)
├── churn_model_complete_pipeline.py    # Python версия (для version control)
├── convert_py_to_notebook.py           # Конвертер .py → .ipynb
├
── data/
│   ├── churn_train_ul.parquet          # Обучающие данные (Parquet)
│   └── churn_prod_ul.parquet           # Production данные (опционально)
├── models/
│   ├── model_1_small_business.cbm      # Модель 1
│   ├── model_2_middle_large_business.cbm  # Модель 2
│   ├── model_1_metadata.json           # Метаданные модели 1
│   ├── model_2_metadata.json           # Метаданные модели 2
│   └── preprocessing_pipeline.pkl      # Preprocessing pipeline
├── figures/
│   ├── 01_eda_target.png               # EDA визуализации
│   ├── 02_models_comparison.png        # Сравнение моделей
│   └── 03_roc_curves_comparison.png    # ROC кривые
└── output/
    └── models_comparison.csv           # Таблица сравнения

```

---

## 🚀 Быстрый старт

### 1. Подготовка данных (если еще не сделано)

Если у вас CSV файлы, конвертируйте их в Parquet:

```bash
# Установить pyarrow
pip install pyarrow

# Конвертировать CSV → Parquet
python csv_to_parquet_converter.py
```

### 2. Запуск notebook

```bash
# Запустить Jupyter
jupyter notebook Churn_Model_Complete.ipynb
```

**В notebook:**
- Меню → `Kernel` → `Restart & Run All`
- Ждать завершения (~10-15 минут)
- Проверить результаты

### 3. Проверка результатов

После выполнения:
- ✓ Модели сохранены в `models/`
- ✓ Метрики выведены в конце notebook
- ✓ Визуализации в `figures/`
- ✓ Сравнение моделей в `output/models_comparison.csv`

---

## 📊 Структура Notebook

| Секция | Описание |
|--------|----------|
| **1. Imports** | Импорт библиотек и конфигурация |
| **2. Data Loading** | Загрузка из Parquet (~5 сек вместо 60!) |
| **3. EDA** | Exploratory Data Analysis |
| **4. Temporal Split** | Разбиение Train/Val/Test-OOT |
| **5. Gap Removal** | Удаление клиентов с пробелами |
| **6. Preprocessing** | Feature engineering, scaling, imputation |
| **7. Segment Split** | Разделение на 2 сегмента |
| **8. Model 1** | Small Business - обучение и оценка |
| **9. Model 2** | Middle+Large Business - обучение и оценка |
| **10. Comparison** | Сравнение моделей |
| **11. Final Report** | Итоговые метрики для банка |
| **12. Saving** | Сохранение моделей и результатов |

---

## 📈 Ожидаемые метрики

### Модель 1: Small Business

```
МЕТРИКИ (Test OOT):
  ROC-AUC:    ~0.89
  GINI:       ~0.78
  PR-AUC:     ~0.18
  Precision:  ~0.23
  Recall:     ~0.25
  F1-Score:   ~0.24
```

### Модель 2: Middle + Large Business

```
МЕТРИКИ (Test OOT):
  ROC-AUC:    ~0.85-0.90
  GINI:       ~0.70-0.80
  PR-AUC:     ~0.10-0.20
  Precision:  ~0.15-0.30
  Recall:     ~0.20-0.35
  F1-Score:   ~0.18-0.28
```

*Точные значения зависят от данных*

---

## 🔧 Технические детали

### Preprocessing Steps:

1. **Temporal Split** - разбиение по времени
2. **Gap Removal** - удаление клиентов с пробелами в наблюдениях
3. **Constant Removal** - удаление константных признаков
4. **Outlier Handling** - обработка выбросов (IQR × 1.5)
5. **Missing Imputation** - заполнение пропусков (median/mode)
6. **Correlation Removal** - удаление коррелирующих признаков (>0.85)

### CatBoost Parameters:

```python
{
    'iterations': 500,
    'learning_rate': 0.05,
    'depth': 4,
    'l2_leaf_reg': 3,
    'min_data_in_leaf': 100,
    'loss_function': 'Logloss',
    'eval_metric': 'AUC',
    'early_stopping_rounds': 100,
    'random_seed': 42
}
```

### Class Imbalance Handling:

- **Class Weights**: автоматический расчет
- **Threshold Tuning**: оптимизация по F1-score

---

## 📦 Требования

### Библиотеки:

```bash
pip install pandas numpy scipy matplotlib seaborn scikit-learn catboost pyarrow
```

### Минимальные версии:

- Python >= 3.7
- pandas >= 1.0
- numpy >= 1.18
- catboost >= 1.0
- pyarrow >= 5.0

---

## 🔄 Воспроизводимость

### Гарантия идентичных результатов:

1. **Fixed Random Seed:**
   ```python
   RANDOM_SEED = 42
   np.random.seed(42)
   ```

2. **Детерминированное разбиение:**
   - Temporal split по датам (не случайный)
   - Фиксированные cutoff даты

3. **Сохранение параметров:**
   - Все параметры в `Config` классе
   - Метаданные в JSON

### Проверка воспроизводимости:

```bash
# Запустить notebook дважды
# Первый раз
jupyter nbconvert --to notebook --execute Churn_Model_Complete.ipynb --output run1.ipynb

# Второй раз
jupyter nbconvert --to notebook --execute Churn_Model_Complete.ipynb --output run2.ipynb

# Сравнить метрики (должны совпадать)
```

---

## 📤 Для отчета банку

### Необходимые файлы:

1. **Notebook:**
   - `Churn_Model_Complete.ipynb` (с выполненными ячейками)

2. **Модели:**
   - `models/model_1_small_business.cbm`
   - `models/model_2_middle_large_business.cbm`
   - `models/model_1_metadata.json`
   - `models/model_2_metadata.json`

3. **Данные (raw):**
   - `data/churn_train_ul.parquet` (или .csv)

4. **Результаты:**
   - `output/models_comparison.csv`
   - `figures/` (все визуализации)

5. **Preprocessing:**
   - `models/preprocessing_pipeline.pkl`

---

## 🎯 Чек-лист для банка

- [ ] Данные конвертированы в Parquet
- [ ] Notebook запущен: `Run All`
- [ ] Все ячейки выполнены без ошибок
- [ ] Модели сохранены в `models/`
- [ ] Метрики соответствуют ожиданиям
- [ ] Визуализации сгенерированы
- [ ] Метаданные содержат все параметры
- [ ] Preprocessing pipeline сохранен
- [ ] Сравнительная таблица создана
- [ ] Документация готова

---

## ⚙️ Модификация notebook

### Изменение параметров:

1. Откройте `churn_model_complete_pipeline.py`
2. Измените параметры в классе `Config`
3. Конвертируйте в notebook:
   ```bash
   python convert_py_to_notebook.py
   ```
4. Запустите обновленный notebook

### Примеры изменений:

```python
# Изменить split
TRAIN_SIZE = 0.80  # было 0.70
VAL_SIZE = 0.10    # было 0.15
TEST_SIZE = 0.10   # было 0.15

# Изменить параметры CatBoost
CATBOOST_PARAMS = {
    'iterations': 1000,  # было 500
    'learning_rate': 0.03,  # было 0.05
    ...
}

# Изменить сегменты
SEGMENT_1_VALUES = ['SMALL_BUSINESS', 'MIDDLE_BUSINESS']
SEGMENT_2_VALUES = ['LARGE_BUSINESS']
```

---

## 🐛 Troubleshooting

### Ошибка: FileNotFoundError

```bash
# Проверить наличие файла
ls data/churn_train_ul.parquet

# Если нет, конвертировать из CSV
python csv_to_parquet_converter.py
```

### Ошибка: Out of Memory

```python
# В Config изменить
HANDLE_OUTLIERS = False  # Отключить для экономии памяти
REMOVE_HIGH_CORRELATIONS = False
```

### Долгое обучение

```python
# Уменьшить iterations
CATBOOST_PARAMS = {
    'iterations': 200,  # вместо 500
    ...
}
```

---

## 📚 Дополнительные материалы

- **Parquet Guide:** `PARQUET_GUIDE.md`
- **CSV Converter:** `csv_to_parquet_converter.py`
- **Python Script:** `churn_model_complete_pipeline.py`
- **Converter:** `convert_py_to_notebook.py`

---

## ✅ Итог

После выполнения `Run All` вы получите:

✓ Две обученные модели CatBoost
✓ Полный набор метрик (GINI, ROC-AUC, и др.)
✓ Визуализации для отчета
✓ Сохраненные модели с метаданными
✓ Воспроизводимые результаты
✓ Документированный процесс

**Готово для предоставления в банк! 🎉**
